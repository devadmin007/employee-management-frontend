import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    common: {
      black: "#000000",
      white: "#ffffff",
    },
  },
  typography: {
    htmlFontSize: 16,
  },
  components:{
    
  }
});
